"use client";
import {useState} from "react";
export default function ImageLightbox({images,classNamePrefix="gallery"}:{images:string[];classNamePrefix?:"gallery"|"floor"}){const[selected,setSelected]=useState<string|null>(null);if(!images.length)return null;if(classNamePrefix==="floor")return <>{<div className="floorPlanGrid">{images.map((image,index)=><div key={`${image}-${index}`} className="floorPlanItem" style={{backgroundImage:`url(${image})`}} onClick={()=>setSelected(image)}/>)}</div>}{selected&&<div className="lightbox" onClick={()=>setSelected(null)}><button className="lightboxClose">Fechar</button><img className="lightboxImage" src={selected} alt="Imagem ampliada"/></div>}</>;return <>{<div className="gallery">{images.slice(0,7).map((image,index)=><div key={`${image}-${index}`} className="galleryItem" style={{backgroundImage:`url(${image})`}} onClick={()=>setSelected(image)}/>)}</div>}{selected&&<div className="lightbox" onClick={()=>setSelected(null)}><button className="lightboxClose">Fechar</button><img className="lightboxImage" src={selected} alt="Imagem ampliada"/></div>}</>}
