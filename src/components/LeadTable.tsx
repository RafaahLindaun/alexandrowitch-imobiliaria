"use client";
import {useState} from "react";import {createClient} from "../lib/supabase/client";import {Lead} from "../types/property";
export default function LeadTable({leads}:{leads:Lead[]}){const supabase=createClient();const[items,setItems]=useState(leads);async function toggleLead(id:string,checked:boolean){setItems(c=>c.map(i=>i.id===id?{...i,checked}:i));await supabase.from("leads").update({checked}).eq("id",id)}return <div><div className="leadRow leadHeader"><strong></strong><strong>Nome</strong><strong>Telefone</strong><strong>E-mail</strong><strong>Status</strong></div>{items.map(lead=><div className={`leadRow ${lead.checked?"checked":""}`} key={lead.id}><input type="checkbox" checked={!!lead.checked} onChange={e=>toggleLead(lead.id,e.target.checked)}/><span>{lead.name}</span><span>{lead.phone||"-"}</span><span>{lead.email||"-"}</span><span>{lead.checked?"Conferido":lead.status||"Novo"}</span></div>)}</div>}
